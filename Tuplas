"""Tuplas"""
colores = ('rojo', 'azul', 'verde', 'amarillo', 'marrón', 'lila', 'negro', 'rosa', 'blanco', 'naranja')
print(colores[1])#Aqui pedimos llamar el segundo dato de la tupla

numeros = (10, 1, 5, 11)

operacion = numeros[0] + numeros[2] + numeros[3] - numeros[1] #sumar los datos respecto a su pocision
#sumamos los carasteres de la tupla para optener 25
print(operacion)
